# DBexec

Be able to run a SQL query

### Installation

With pathogen
```sh
cd .vim/bundle/
git clone https://github.com/swrd06bp/DBexec
```

Add the following lines in your `.vimrc`
```sh
" DBexec
let g:DBexec = 'production'
let g:creds = {}
let g:creds.production = {}
let g:creds.production.hostname = ''
let g:creds.production.database = ''
let g:creds.production.username = ''
let g:creds.production.password = ''
let g:creds.production.verification = 1"
```



### Usage

:<localleader>d or \d to run the query

